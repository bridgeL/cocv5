from skill import Skill


class CoCCharacterGeneratorSkill(Skill):
    """克苏鲁的呼唤7版角色卡生成技能"""

    name = "CoC 7版角色生成"
    desc = "严格按照《克苏鲁的呼唤》第七版规则，利用掷骰工具生成准确的角色基础属性与派生属性"
    content = """
#### **Workflow Logic (Workflow):**

1.  **第一阶段：生成基础属性**
    **必须首先调用** `coc_character_attributes` 工具一次性生成所有基础属性。
    该工具会自动完成：
    *   生成 STR/CON/DEX/APP/POW（3d6 × 5）
    *   生成 SIZ/INT/EDU（(2d6+6) × 5）
    *   生成 Luck（3d6 × 5）
    *   计算所有属性的半值和五分之一值
    *   计算派生属性（HP/MP/SAN/MOV/Build/DB）

2.  **第二阶段：年龄调整逻辑（默认 20-39 岁）**
    基于第一阶段生成的属性，除非玩家另有要求，否则按标准成年人（20-39岁）处理：
    *   **教育增强检定**：调用 `roll_dice(expression="1d100")`。若结果 > 当前教育值，则调用 `roll_dice(expression="1d10")` 并加到教育属性上（上限 99）。
    *   **年龄减值**（若年龄 ≥ 40）：根据《守秘人规则书》第 31 页的年龄调整表执行属性减损。

3.  **第三阶段：输出角色卡**
    将最终结果按照格式返回给玩家。

#### **Format of Response (Output):**

请严格按照以下格式返回结果：

**【调查员基础属性】**
*   力量：[全额]/[半值]/[五分之值]
*   体质：[全额]/[半值]/[五分之值]
*   ...（以此类推）
*   教育：[最终增强后的全额值]
*   幸运：[全额值]

**【调查员派生数值】**
*   HP：[数值] | MP：[数值] | SAN：[数值]
*   MOV：[数值] | Build：[数值] | DB：[数值]

---

### **AI 内部指令提示词 (Prompts for Internal Logic):**

*   **原则：** 永远不要手动"发明"掷骰结果。必须调用工具获取结果。
*   **第一步必做：** 首先调用 `coc_character_attributes` 生成基础属性。
*   **计算精准：** 严格执行向下取整。
*   **年龄判定：** 默认设置为 20-39 岁。

**Analogy（类比）：**
作为"角色卡建筑师"，你就像一台**3D打印机**。`coc_character_attributes` MCP 工具为你提供完整的调查员骨架，你只需根据玩家需求（年龄）进行微调，然后交付成品。
"""
